module full_adder_PG
(
	input logic a,
	input logic b,
	input logic Cin,
	output logic Cout,
	output logic s,
	output logic p,
	output logic g

);

	always_comb begin
	
	s = a ^ b ^ Cin;
	Cout = (a & b) | (a & Cin) | ( b & Cin);
	
	end
	
endmodule